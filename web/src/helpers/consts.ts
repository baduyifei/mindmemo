// UNKNOWN_ID is the symbol for unknown id.
export const UNKNOWN_ID = -1;

// ANIMATION_DURATION is the duration of animation in milliseconds.
export const ANIMATION_DURATION = 200;

// DAILY_TIMESTAMP is the timestamp for a day.
export const DAILY_TIMESTAMP = 3600 * 24 * 1000;

// TAB_SPACE_WIDTH is the default tab space width.
export const TAB_SPACE_WIDTH = 2;

// DEFAULT_LIST_MEMOS_PAGE_SIZE is the default page size for list memos request.
export const DEFAULT_LIST_MEMOS_PAGE_SIZE = 16;

// DEFAULT_SERVICE_NAME is the product name shown when no custom service name is configured.
export const DEFAULT_SERVICE_NAME = "MindMemo";

// Content-versioned built-in assets avoid stale browser favicon and avatar caches after upgrades.
export const DEFAULT_LOGO_URL = "/logo.webp?v=9558c4a540cc";
export const DEFAULT_FULL_LOGO_URL = "/full-logo.webp?v=47b7a81012b8";
export const DEFAULT_EMPTY_STATE_URL = "/mindmemo-empty.svg?v=dabf3fa374d4";
